#include <unistd.h>
#include <stdlib.h>
#include <stdio.h>
#include <fcntl.h>

int main()
{
	char *str = "hello world";
	//char *arr[] = {"/bin/ls", "~q", NULL};
	//int fd = open("main.c", O_RDONLY);
	//dup2(fd, 0);
	//execve(arr[0], arr, NULL);
}